<PRE>
<?php

    // arithmatic oprator
    $a=20;
    $b=5;

    $add = $a + $b;
    $sub = $a - $b;
    $mul = $a * $b;
    $div = $a / $b;
    $mod = $a % $b;

    echo "++++++ Arithmetic Operators ++++++" . "<br>";
    echo "Adition :" .$add . "<br>";
    echo "Subtraction :" .$sub . "<br>";
    echo "Multiplication :" .$mul . "<br>";
    echo "Divison :" .$div . "<br>";
    echo "Modulo :" .$mod . "<br>"."<br>";

    // Assignment operator
    $x=20;
    $y=10;
    $x += $y; // $x = $x+$y;
    echo "++++++ Assignment Operators ++++++"."<br>";
    echo "x += y: ".$x ."<br>"."<br>";

    //comparison oprator 
    $p=10;
    $q=20;
    echo "++++++ Comparison Operators ++++++"."<br>";
    echo "Equal to (p == q) :".($p == $q) . "<br>";
    echo "Not Equal to (p != q) :".($p != $q) . "<br>";
    echo "Greater than (p > q) :".($p > $q) . "<br>";
    echo "Less than (p < q) :".($p < $q) . "<br>";
    echo "Greater than Equal to (p >= q) :".($p >= $q) . "<br>";
    echo "Less than Equal to (p <= q) :".($p <= $q) . "<br>"."<br>";

    // logical oprator 
    $bool1 = true;
    $bool2 = false;
    echo "++++++ Logical Operators ++++++"."<br>";
    echo "AND (bool1 && boll2) :" .($bool1 && $bool2)."<br>";
    echo "AND (bool1 && boll2) :" .($bool1 && $bool2)."<br>";
    echo "AND (bool1 && boll2) :" .($bool1 && $bool2)."<br>";


?>

</PRE>
    